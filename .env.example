# Smart Money v2 uses MCP remote mode — no local env vars needed.
# All configuration is on the Antalpha server side.
#
# Agent ID and API key are auto-generated on first use and stored at:
#   ~/.smart-money/agent.json
